
indexes:
  - name: GSI1
    tableName: MainTable
    throughput:
      readCapacityUnits: 5
      writeCapacityUnits: 5
    keys:
      partitionKey:
        name: GSI1PK
        type: string
      sortKey:
        name: GSI1SK
        type: string
    projection:
      type: all
